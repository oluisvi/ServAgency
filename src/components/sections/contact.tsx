"use client"; import {FormEvent} from "react"; import {ArrowUpRight} from "lucide-react";
export function Contact(){function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const txt=`Olá! Conheci a ServAgency pelo site.%0A%0ANome: ${f.get("name")}%0AEmpresa: ${f.get("company")}%0AContato: ${f.get("contact")}%0AProjeto: ${f.get("message")}`;window.open(`https://wa.me/5512992568583?text=${txt}`,"_blank")};return <section className="contact" id="contato"><div><span>07 / START A PROJECT</span><h2>Tem um problema, uma ideia ou um processo travado?</h2><p>Vamos definir a rota.</p></div><form onSubmit={submit}><input name="name" required placeholder="Seu nome"/><input name="company" placeholder="Empresa"/><input name="contact" required placeholder="WhatsApp ou e-mail"/><textarea name="message" required minLength={10} placeholder="Conte um pouco sobre o que precisa mudar"/><button>Preparar mensagem <ArrowUpRight/></button></form></section>}
