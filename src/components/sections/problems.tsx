/**
 * Legacy compatibility shim.
 * Problems were replaced by the Outcomes section in Digital Systems in Motion.
 * Kept temporarily so repository-overwrite uploads cannot leave an old,
 * type-incompatible file behind.
 */
export function Problems() {
  return null;
}
